# Documentação Deeper: Migrações para Tarefas Agendadas

Este diretório contém a documentação e os exemplos de código Elixir para as migrações do banco de dados SQLite relacionadas à tabela `sys_cron_jobs`.

## Migrações Definidas:

1.  [**Criar Tabela `sys_cron_jobs` (`create_sys_cron_jobs_table.elixir.md`)**](./create_sys_cron_jobs_table.elixir.md):
    *   Cria a tabela `sys_cron_jobs`, que define as tarefas agendadas (cron jobs) do sistema UNA.